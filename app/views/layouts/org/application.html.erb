<!DOCTYPE html>
<html lang="ja">
  <head>
    <title><%= content_for?(:page_title) ? "Umaxica | #{yield(:page_title)} " : "Umaxica" %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= favicon_link_tag "org/favicon.ico" %>
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= stylesheet_link_tag "org/main" %>
    <%= stylesheet_link_tag "tailwind", "application", "data-turbo-track": "reload" %>
    </head>

  <body class="px-4">
    <header>
      <h1 class="container mx-auto mt-28 px-5 flex"><%= link_to 'UMAXICA(STAFF)', org_root_path %></h1>
    </header>

    <nav id="app" class="mt-4 bg-white bg-opacity-50 hover:font-black hover:text-blue-500"></nav>

    <main class="container mx-auto mt-28 px-5 block"><%= yield %></main>

    <footer>
      <%= link_to 'Homepage', org_root_path %>
    </footer>

    <%= javascript_include_tag "org/main" %>
  </body>
</html>
