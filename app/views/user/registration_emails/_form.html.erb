<% content_for(:page_title, 'Sign up with email') %>

<%= form_with model: @user_email, url: user_registration_emails_path do |form| %>
  <% if user_email.errors.any? %>
    <div>
      <h2><%= pluralize(user_email.errors.count, "error") %> prohibited this email address from being saved:</h2>

      <ul>
        <% user_email.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :address, style: "display: block" %>
    <%= form.email_field :address %>
  </div>

  <div>
    <%= form.submit class: "bg-blue-700 hover:bg-blue-700 text-white " %>
  </div>
<% end %>
