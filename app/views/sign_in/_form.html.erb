<%= form_with model: @user_email, url: sign_in_index_path do |form| %>
  <% if @user_email.errors.any? %>
    <div>
      <h2><%= pluralize(@user_email.errors.count, "error") %> prohibited this email address from being saved:</h2>

      <ul>
        <% @user_email.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :address, style: "display: block" %>
    <%= form.text_field :address %>
  </div>

  <div>
    <%= form.label :password_field, style: "display: block" %>
    <%= form.text_field :password_field %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
