name: Hello
on:
  workflow_dispatch:
    inputs:
      greeting:
        type: string
        default: Hello
        required: true
        description: ???
jobs:
  hello:
    name: 'First Action!'
    runs-on: ubuntu-latest
    steps:
      - run: |
          ls
          pwd
          echo "hello, world! ${{inputs.greeting}}"
      - uses: actions/checkout@v4
      - run: |
          ls
          pwd
          echo "hello, world! ${{inputs.greeting}}"
