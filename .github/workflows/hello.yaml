name: Hello
on: workflow_dispach:
    inputs:
      greeting:
        type: string
        default: Hello
        required: true
        description: ???
jobs:
  hello:
    name: 'First Action!'
    runs-on: ubuntu-latest
    steps:
      - run: |
          ls
          pwd
          echo "hello, world!"
      - uses: actions/checkout@v4
